# ═══════════════════════════════════════════════════════════════
#  Ai_Alabbasi — التبعيات (Dependencies)
# ───────────────────────────────────────────────────────────────
#  النظام يعتمد بشكل شبه كامل على مكتبة Python القياسية (stdlib):
#  json, sqlite3, urllib, subprocess, pathlib, re, os, sys, datetime, time
#  التبعية الخارجية الوحيدة المطلوبة هي PyYAML (لقراءة إعداد العقل).
# ═══════════════════════════════════════════════════════════════

PyYAML>=6.0

# ───────────────────────────────────────────────────────────────
#  اختياري — للواجهة الويب فقط (web/server.py)
#  ثبّتها فقط إذا أردت تشغيل واجهة المحادثة:
#      pip install fastapi uvicorn python-multipart
# ───────────────────────────────────────────────────────────────
# fastapi>=0.110
# uvicorn>=0.29
# python-multipart>=0.0.9   # مطلوبة لرفع الملفات/الصور في الواجهة

# ───────────────────────────────────────────────────────────────
#  اختياري — لرفع جودة البحث الهجين (Phase 2)
#  بدونها يعمل النظام بمُضمِّن خفيف نقيّ بايثون (hashing) تلقائياً.
#  للجودة الأعلى (تمثيلات دلالية حقيقية):
#      pip install sentence-transformers
# ───────────────────────────────────────────────────────────────
# sentence-transformers>=2.2   # محرّك embeddings (all-MiniLM-L6-v2)
